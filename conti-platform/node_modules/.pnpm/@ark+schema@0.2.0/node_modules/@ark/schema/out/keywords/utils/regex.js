import { defineRoot } from "../../scope.js";
export const defineRegex = (regex, description) => defineRoot({
    domain: "string",
    pattern: {
        rule: regex.source,
        flags: regex.flags,
        description
    }
});
